#include <stdio.h>

int main(void) {

  int k, e, m, sum;

  printf("국어 점수 입력: ");
  scanf("%d", &k);
  printf("영어 점수 입력: ");
  scanf("%d", &e);
  printf("수학 점수 입력: ");
  scanf("%d", &m);

  sum = k + e + m;

  printf("==> 합계 %d, 평균 %.2f입니다.", sum, sum / 3.0);
  return 0;
}
